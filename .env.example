# MySQL connection used by the FastAPI backend.
DATABASE_URL=mysql+pymysql://insightiq_user:change_me@localhost:3306/insightiq

# Generate a unique value before running outside local development.
SECRET_KEY=replace_with_a_long_random_secret
ACCESS_TOKEN_EXPIRE_MINUTES=60
